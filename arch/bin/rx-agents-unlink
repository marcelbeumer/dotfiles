#!/usr/bin/env bash
set -euo pipefail

unlink() {
  local dst="$1"
  if [[ -L "$dst" ]]; then
    rm "$dst"
    echo "unlinked $dst"
  else
    echo "not linked: $dst"
  fi
}

unlink "$HOME/.config/opencode"
unlink "$HOME/.pi"
