#!/usr/bin/env bash
current="${HOME}/.local/share/wallpaper/.current"

args=()
if [ -f "${current}" ]; then
  path=$(cat "${current}")
  args=(-i "${path}")
fi

swaylock "${args[@]}" -f --indicator-idle-visible --ring-color 000000DD
