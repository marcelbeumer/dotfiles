#!/usr/bin/env bash
dir="${HOME}/.local/share/wallpapers"
current="${dir}/.current"

args=()
if [ -f "${current}" ]; then
  name=$(cat "${current}")
  args=(-i "${dir}/${name}")
fi

swaylock "${args[@]}" -f
